spec_id: v1
spec_text_ref: c8dd44c0c032e5be2afea1e8804b7ff368b02934b66e5d1036d0e3183f1cb423
spec_text_sha: 0503c942d4e8ade09f391e27c1e4829526eded24
revision: 2
project_status: in_progress
current_phase_id: phase-1
created_at: '2026-05-17T14:18:26.163102Z'
updated_at: '2026-05-17T14:23:47.027515Z'
phases:
- id: phase-1
  name: Add function and tests
  summary: 'When this phase closes, the repo has add.py at the project root containing a single add(a:
    int, b: int) -> int function that returns a + b and raises TypeError on non-int input, plus test_add.py
    with parametrized fixtures covering positive, negative, zero, and type-error cases. The closing checkpoint
    can verify by running the test suite and inspecting both files at their specified paths.'
  status: in_progress
  position: 1
  created_at: '2026-05-17T14:18:26.163102Z'
  items:
  - id: v014smoke-item-001
    title: Implement add() and its parametrized test suite
    summary: 'Create add.py at the project root exposing add(a: int, b: int) -> int that returns a + b
      and raises TypeError when either argument is not an int. Create test_add.py with parametrized pytest
      fixtures covering positive integers, negative integers, zero, and TypeError on non-int input.'
    scope_notes: 'Should include: add.py with the function and its signature, explicit int type checks
      raising TypeError, test_add.py using pytest.mark.parametrize, parametrized cases for positive/negative/zero/type-error.
      Should NOT include: package structure (pyproject.toml, src/ layout, __init__.py), CLI wrapper, additional
      arithmetic operations, type-coercion behavior beyond strict int check, custom exception subclasses.'
    prior_deliverable_refs: []
    rationale: Spec is small and atomic — one function and its tests form a single coherent PR. Below
      the 200-LOC heuristic, but splitting into implementation-then-tests would produce ceremony without
      checkpoint value and an intermediate state (function with no tests) that has no meaningful verification
      surface.
    status: in_haggle
    supersedes: []
    superseded_by: []
    estimated_size: small
